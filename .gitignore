.gradle/
build/
app/build/
*.apk
.DS_Store
.idea/
*.iml
local.properties

# UI/UX skill caches
.claude/skills/ui-ux-pro-max/scripts/__pycache__/

# Release signing secrets — keep ONLY on local machines, never commit
/keystore.properties
/app/release.keystore
*.jks

# Editor / tooling / local debug — keep local, don't commit
.claude/
.vscode/
.dbg/
